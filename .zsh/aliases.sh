alias \
    l="ls -a" \
    mkdir="mkdir -p" \
    q="exit" \
    :q="exit" \
    c="cd" \
    c.="cd $HOME/code/dots/" \
    cc="cd $HOME/code/" \
    cs="cd $HOME/code/drafts/" \
    so.="source $HOME/.zshrc" \
    v="nvim" \
    vi="nvim" \
    env="nvim .env" \
    venv="source venv/bin/activate" \
    unzip="extract" \
    off="shutdown now" \

    t="tmuxifier" \
    tls="tmuxifier list-sessions"
